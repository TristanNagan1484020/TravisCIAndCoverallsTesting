language: python
python:
  - "3.6"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coverage
  - pip install coveralls
script:
  - py.test --cov . --cov-report term-missing
after_success: 
  - coveralls 
